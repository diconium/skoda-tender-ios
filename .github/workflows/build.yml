name: format, lint, build and test

on:
    pull_request:
      branches:
      - main

jobs:
  build_project:
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: SwiftFormat
        run: swiftformat . --lint
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: SwiftLint
        run: swiftlint lint --strict
      - name: Build Cleanup
        run: |
          xcodebuild clean -project skoda-tender-ios.xcodeproj -scheme GitHubAction | xcpretty
      - name: Build Test
        run: |
          xcodebuild test -project skoda-tender-ios.xcodeproj -scheme GitHubAction -destination 'platform=iOS   Simulator,name=iPhone 14,OS=16.2' | xcpretty -s

